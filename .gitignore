# Internal dev docs
CLAUDE.md

# Virtual environment
venv/

# Python cache
__pycache__/
*.pyc
*.pyo

# Local config (created on first run)
prefs.json
last_used.json
.last_used.json

# User presets (per-machine, never repo content). The BUILT-IN presets live in code
# (BUILT_IN_PRESETS / KREA2_BUILT_IN_PRESETS in lora_trainer_gui.py), so nothing here
# needs shipping — only the folders themselves, kept alive by their .gitkeep.
presets/.last_train_settings.json
presets/**/*.json
!presets/**/.gitkeep

# Dataset TOMLs (auto-generated)
dataset/*
!dataset/.gitkeep

# Diagnostic dumps
repair_studio_last_preview.log

# Training output / cache (large files)
output_loras/*
!output_loras/.gitkeep
cache/*
!cache/.gitkeep
profiles/*
!profiles/.gitkeep

# Pause state
.fizgig_paused.json

# Model files (user-provided)
*.safetensors
# ...except the bundled silu(t_emb) grid (5.5 MB, Apache-2.0, from larryvrh's
# ComfyUI-MiniMax-H3-Turbo node): the pruned base cannot compute silu(t_emb), and the
# Turbo LoRA's AdaLN injection for previews needs the full model's answer.
!src/fizgig/assets/h3_silu_temb_grid.safetensors
*.ckpt
*.bin

# Research (private)
research/
anchor_images/

# IDE
.vscode/
.idea/

# OS files
.DS_Store
Thumbs.db
nul

# Benchmark scratch (bench_train.py writes LoRAs + results here)
_bench_out/
bench_results.json
repair_studio_last_preview.log

# Checkpoint-to-LoRA utility state (per-machine)
.diff_to_lora.json

# Internal performance notes (not for publication)
docs/PERF_ROADMAP.md
docs/PERF_CHANGES.md
docs/bench_krea2_baseline.json

# Internal design notes (not for publication)
docs/PHONE_MONITOR_PLAN.md
docs/MINIMAX_REFERENCE_NOTES.md
docs/MINIMAX_BLOCKS.md

# Written by launch.pyw when the app fails to start
launch_error.log

# Written by install_fizgig_rocm.bat / install_fizgig_rocm.sh (BNB_ROCM_VERSION / ROCM_PATH)
rocm_env.bat
rocm_env.sh
gpu_detect_debug.log

# Leftover portable-Python cache from older ROCm installer attempts
python312/

# Browser job records and runtime state
.fizgig/

# Browser frontend build artifacts and installed dependencies
web/node_modules/
web/dist/
web/*.tsbuildinfo
web/vite.config.js
web/vite.config.d.ts


# Internal-only: debugging test harnesses + template setup notes (local, never public)
tests/
docs/RUNPOD_TEMPLATE.md

# Local-only deploy/sync helpers (never commit)
sync-to-remote.sh
gizmo_settings.json
docs/RESEARCH_*
docs/DESIGN_component_ft_24gb.md
docs/EVAL_full_model_h3_video.md
docs/FINETUNE_ROTATION.md
docs/DISTRIBUTED_FINETUNE.md
